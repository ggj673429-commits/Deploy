╔══════════════════════════════════════════════════════════════╗
║          🚀 DEPLOY TO RAILWAY - QUICK COMMANDS              ║
╚══════════════════════════════════════════════════════════════╝

Your Gaming Platform is ready for Railway deployment!
Railway fully supports PostgreSQL - no code changes needed! ✅

═══════════════════════════════════════════════════════════════
              📋 DEPLOYMENT COMMANDS
═══════════════════════════════════════════════════════════════

Run these commands in your terminal from the /app directory:

1. Install Railway CLI:
   ────────────────────────────────────────────────────────────
   npm i -g @railway/cli


2. Login to Railway:
   ────────────────────────────────────────────────────────────
   railway login
   
   → Browser will open for authentication


3. Initialize Project:
   ────────────────────────────────────────────────────────────
   railway init
   
   → Creates new Railway project


4. Add PostgreSQL Database:
   ────────────────────────────────────────────────────────────
   railway add
   
   → Select "PostgreSQL" from the list


5. Set Environment Variables:
   ────────────────────────────────────────────────────────────
   # Generate JWT secret
   openssl rand -hex 32
   
   # Set it (replace with generated value)
   railway variables set JWT_SECRET_KEY=your-generated-secret-here
   
   # Optional: Add Telegram integration
   railway variables set TELEGRAM_BOT_TOKEN=your-telegram-token
   railway variables set TELEGRAM_CHAT_ID=your-telegram-chat-id


6. Deploy Application:
   ────────────────────────────────────────────────────────────
   railway up
   
   → Deploys backend + frontend + database


7. Run Database Migrations:
   ────────────────────────────────────────────────────────────
   railway run alembic upgrade head
   
   → Sets up database schema


8. Create Admin User:
   ────────────────────────────────────────────────────────────
   railway run python scripts/create_admin.py
   
   → Creates default admin account


9. Open Your Live App:
   ────────────────────────────────────────────────────────────
   railway open
   
   → Opens your gaming platform in browser! 🎉


═══════════════════════════════════════════════════════════════
              🔍 USEFUL RAILWAY COMMANDS
═══════════════════════════════════════════════════════════════

View logs:           railway logs
Check status:        railway status
List services:       railway service
View variables:      railway variables
Run command:         railway run <command>
Connect to DB:       railway run psql

═══════════════════════════════════════════════════════════════
              ⚙️  ENVIRONMENT VARIABLES REQUIRED
═══════════════════════════════════════════════════════════════

✅ AUTOMATICALLY SET BY RAILWAY:
   • DATABASE_URL         (PostgreSQL connection)
   • REACT_APP_BACKEND_URL (Your backend URL)

✅ YOU NEED TO SET:
   • JWT_SECRET_KEY       (Generate: openssl rand -hex 32)

✅ OPTIONAL (For full features):
   • TELEGRAM_BOT_TOKEN   (From @BotFather on Telegram)
   • TELEGRAM_CHAT_ID     (From @userinfobot on Telegram)

═══════════════════════════════════════════════════════════════
              🎯 WHAT RAILWAY PROVIDES
═══════════════════════════════════════════════════════════════

✅ PostgreSQL Database (automatically configured)
✅ Backend hosting (FastAPI on port 8001)
✅ Frontend hosting (React build)
✅ HTTPS/SSL certificates
✅ Custom domain support
✅ Auto-scaling
✅ Zero-downtime deployments
✅ Environment management

Cost: $5/month per project

═══════════════════════════════════════════════════════════════
              ✨ QUICK START (Copy & Paste)
═══════════════════════════════════════════════════════════════

# Install & login
npm i -g @railway/cli && railway login

# Create project & add database
railway init && railway add

# Generate and set JWT secret
export JWT_SECRET=$(openssl rand -hex 32)
railway variables set JWT_SECRET_KEY=$JWT_SECRET

# Deploy!
railway up

# Setup database
railway run alembic upgrade head
railway run python scripts/create_admin.py

# Open your app
railway open

🎉 Done! Your Gaming Platform is LIVE!

═══════════════════════════════════════════════════════════════
              🔧 TROUBLESHOOTING
═══════════════════════════════════════════════════════════════

❌ Problem: "railway command not found"
   Fix: Run npm i -g @railway/cli

❌ Problem: "Database connection failed"
   Fix: Make sure you ran "railway add" and selected PostgreSQL

❌ Problem: "JWT_SECRET_KEY not set"
   Fix: Run railway variables set JWT_SECRET_KEY=$(openssl rand -hex 32)

❌ Problem: "Migrations failed"
   Fix: Check database is running with "railway status"

❌ Problem: "Can't access application"
   Fix: Run "railway logs" to see errors

═══════════════════════════════════════════════════════════════
              📚 MORE HELP
═══════════════════════════════════════════════════════════════

Railway Docs:     https://docs.railway.app
Your Dashboard:   https://railway.app/dashboard
Support:          https://discord.gg/railway

Full Documentation: See DEPLOYMENT_READY.md in this folder

═══════════════════════════════════════════════════════════════

              Ready to deploy? Run the Quick Start commands! 🚀
              
═══════════════════════════════════════════════════════════════
